import { DisableIfReqInProgressDirective } from './disable-if-req-in-progress.directive';

describe('DisableIfReqInProgressDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableIfReqInProgressDirective();
    expect(directive).toBeTruthy();
  });
});
